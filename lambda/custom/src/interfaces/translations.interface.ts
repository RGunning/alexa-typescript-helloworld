import * as i18next from "i18next";

export interface Translations {
  en: { [index: string]: string };
  "en-GB"?: { [index: string]: string };
  "en-US"?: { [index: string]: string };
  de?: { [index: string]: string };
}
